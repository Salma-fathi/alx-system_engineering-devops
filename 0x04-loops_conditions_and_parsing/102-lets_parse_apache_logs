#!/usr/bin/env bash

# Bash script that displays the visitor IP along with the HTTP status
# code from the Apache log file.

LOG_FILE="/path/to/apache-access.log"
awk '{print $1 " " $9}' "$LOG_FILE" | tail -n 10
